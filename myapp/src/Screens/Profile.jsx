import React from 'react'
import IndexProfile from '../Components/MyProfile/IndexProfile'

const Profile = ({setLogout}) => {
    return (
        <>
            <IndexProfile setLogout = {setLogout} />
        </>
    )
}

export default Profile
